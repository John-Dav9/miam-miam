       
<% @recettes.each do |recette| %>
   <div>
       <h2><%= link_to recette.title, recette_path(recette) %></h2>         
       <% if recette.image.attached? %>
           <%= image_tag url_for(recette.image) %>          
       <% end %>
   </div>
<% end %>